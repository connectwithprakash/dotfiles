#!/usr/bin/env bash
# Dotfiles CLI - Main entry point for managing dotfiles
#
# Usage:
#   dotfiles                # Launch interactive menu
#   dotfiles sync           # Sync dotfiles
#   dotfiles install        # Install components
#   dotfiles status         # Show status
#   dotfiles health         # Run health check
#   dotfiles help           # Show help

set -e

# Get the repository directory even when this CLI is invoked through a symlink
# such as ~/.local/bin/dotfiles. macOS readlink does not support -f, so resolve
# links manually.
SOURCE="${BASH_SOURCE[0]}"
while [ -L "$SOURCE" ]; do
  SOURCE_DIR="$(cd "$(dirname "$SOURCE")" && pwd)"
  TARGET="$(readlink "$SOURCE")"
  if [[ "$TARGET" == /* ]]; then
    SOURCE="$TARGET"
  else
    SOURCE="$SOURCE_DIR/$TARGET"
  fi
done
DOTFILES_DIR="$(cd "$(dirname "$SOURCE")" && pwd)"
unset SOURCE SOURCE_DIR TARGET
# shellcheck source=scripts/managed_dotfiles.sh
source "$DOTFILES_DIR/scripts/managed_dotfiles.sh"

# Colors (used in print_* functions and inline output)
# shellcheck disable=SC2034
GREEN='\033[0;32m' BLUE='\033[0;34m' YELLOW='\033[1;33m'
RED='\033[0;31m' CYAN='\033[0;36m' GRAY='\033[0;90m' NC='\033[0m'

# Check if gum is available
HAS_GUM=false
if command -v gum &> /dev/null; then
  HAS_GUM=true
fi

# Helper functions
print_header() {
  if [ "$HAS_GUM" = true ]; then
    gum style \
      --border double \
      --border-foreground 212 \
      --padding "0 2" \
      --margin "1 0" \
      "$1"
  else
    echo ""
    echo -e "${BLUE}--- $1 ---${NC}"
    echo ""
  fi
}

print_success() { echo -e "${GREEN}[ok]${NC} $1"; }
print_error() { echo -e "${RED}[error]${NC} $1"; }
print_warning() { echo -e "${YELLOW}[warn]${NC} $1"; }
print_info() { echo -e "${CYAN}[info]${NC} $1"; }

# Show help
show_help() {
  print_header "Dotfiles Manager - Help"

  echo -e "${CYAN}Usage:${NC}"
  echo "  dotfiles [command]"
  echo ""
  echo -e "${CYAN}Commands:${NC}"
  echo -e "  ${GREEN}sync${NC}       Sync dotfiles interactively"
  echo -e "  ${GREEN}apply${NC}      Apply repo dotfiles to home non-interactively"
  echo -e "  ${GREEN}capture${NC}    Capture home dotfiles into repo non-interactively"
  echo -e "  ${GREEN}diff${NC}       Show repo-vs-home differences for managed dotfiles"
  echo -e "  ${GREEN}list${NC}       List managed dotfiles and live paths"
  echo -e "  ${GREEN}install${NC}    Install or update components"
  echo -e "  ${GREEN}update${NC}     Update all installed tools and plugins"
  echo -e "  ${GREEN}status${NC}     Show current status of dotfiles and components"
  echo -e "  ${GREEN}doctor${NC}     Diagnose setup drift with suggested fixes"
  echo -e "  ${GREEN}health${NC}     Run basic health check on installed components"
  echo -e "  ${GREEN}help${NC}       Show this help message"
  echo ""
  echo -e "${CYAN}Examples:${NC}"
  echo "  dotfiles              # Launch interactive menu"
  echo "  dotfiles sync         # Sync dotfiles interactively"
  echo "  dotfiles apply        # Apply all repo-managed dotfiles to home"
  echo "  dotfiles apply zsh/starship.toml"
  echo "  dotfiles capture .aliases"
  echo "  dotfiles diff"
  echo "  dotfiles diff zsh/starship.toml"
  echo "  dotfiles install      # Install components interactively"
  echo "  dotfiles install zsh  # Install/apply only Zsh + Starship"
  echo "  dotfiles install --dry-run zsh iterm2"
  echo "  dotfiles status       # Show current status"
  echo "  dotfiles doctor       # Diagnose drift and suggested fixes"
  echo "  dotfiles health       # Run basic health check"
}

# Resolve optional command arguments to managed dotfiles. With no arguments,
# operate on every managed file.
resolve_managed_targets() {
  resolved_targets=()

  if [ "$#" -eq 0 ]; then
    resolved_targets=("${managed_dotfiles[@]}")
    return 0
  fi

  local requested file matched
  for requested in "$@"; do
    matched=false
    for file in "${managed_dotfiles[@]}"; do
      if [ "$requested" = "$file" ] || [ "$requested" = "$(basename "$file")" ]; then
        resolved_targets+=("$file")
        matched=true
        break
      fi
    done
    if [ "$matched" = false ]; then
      print_error "Unknown managed dotfile: $requested"
      print_info "Run 'dotfiles list' to see valid names."
      return 1
    fi
  done
}

list_managed_dotfiles() {
  print_header "Managed Dotfiles"
  local file
  for file in "${managed_dotfiles[@]}"; do
    echo "$file -> $(get_home_path "$file")"
  done
}

backup_live_file() {
  local home_path="$1"
  [ -e "$home_path" ] || return 0

  local backup_dir
  backup_dir="$HOME/.dotfiles-backup/$(date +%Y-%m-%d_%H%M%S)"
  local relative_path="${home_path#"$HOME"/}"
  local backup_path="$backup_dir/$relative_path"
  mkdir -p "$(dirname "$backup_path")"
  cp "$home_path" "$backup_path"
}

apply_repo_to_home() {
  resolve_managed_targets "$@" || return 1
  local file repo_path home_path label changed=0 skipped=0

  for file in "${resolved_targets[@]}"; do
    repo_path="$DOTFILES_DIR/$file"
    home_path="$(get_home_path "$file")"
    label="$(get_dotfile_label "$file")"

    if [ ! -f "$repo_path" ]; then
      print_error "$label missing in repository"
      continue
    fi

    if [ -f "$home_path" ] && diff -q "$home_path" "$repo_path" >/dev/null 2>&1; then
      echo "No changes in $label (skipped)"
      skipped=$((skipped + 1))
      continue
    fi

    backup_live_file "$home_path"
    mkdir -p "$(dirname "$home_path")"
    cp "$repo_path" "$home_path"
    echo "Applied $label to $home_path"
    changed=$((changed + 1))
  done

  print_success "Applied $changed file(s); skipped $skipped unchanged file(s)."
}

capture_home_to_repo() {
  resolve_managed_targets "$@" || return 1
  local file repo_path home_path label changed=0 skipped=0

  for file in "${resolved_targets[@]}"; do
    repo_path="$DOTFILES_DIR/$file"
    home_path="$(get_home_path "$file")"
    label="$(get_dotfile_label "$file")"

    if [ ! -f "$home_path" ]; then
      print_error "$label missing at $home_path"
      continue
    fi

    if [ -f "$repo_path" ] && diff -q "$home_path" "$repo_path" >/dev/null 2>&1; then
      echo "No changes in $label (skipped)"
      skipped=$((skipped + 1))
      continue
    fi

    mkdir -p "$(dirname "$repo_path")"
    cp "$home_path" "$repo_path"
    echo "Captured $home_path to $label"
    changed=$((changed + 1))
  done

  print_success "Captured $changed file(s); skipped $skipped unchanged file(s)."
}

show_dotfile_diffs() {
  resolve_managed_targets "$@" || return 1
  local file repo_path home_path label changed=0 missing=0 unchanged=0

  for file in "${resolved_targets[@]}"; do
    repo_path="$DOTFILES_DIR/$file"
    home_path="$(get_home_path "$file")"
    label="$(get_dotfile_label "$file")"

    if [ ! -e "$repo_path" ] || [ ! -e "$home_path" ]; then
      print_error "$label missing on one side"
      echo "  Home: $home_path"
      echo "  Repo: $repo_path"
      missing=$((missing + 1))
      continue
    fi

    if diff -q "$home_path" "$repo_path" >/dev/null 2>&1; then
      unchanged=$((unchanged + 1))
      continue
    fi

    changed=$((changed + 1))
    print_header "Diff: $label"
    echo "Home: $home_path"
    echo "Repo: $repo_path"
    echo ""
    diff -u --label "home:$home_path" --label "repo:$repo_path" "$home_path" "$repo_path" || true
    echo ""
  done

  if [ "$changed" -eq 0 ] && [ "$missing" -eq 0 ]; then
    print_success "No differences across ${#resolved_targets[@]} managed dotfile(s)."
    return 0
  fi

  print_info "$changed changed, $missing missing, $unchanged unchanged managed dotfile(s)."
  return 1
}

# Show status
show_status() {
  print_header "Dotfiles Status"

  echo -e "${CYAN}Installed Components:${NC}"

  # Zsh
  if command -v zsh &> /dev/null; then
    local zsh_ver
    zsh_ver=$(zsh --version | awk '{print $2}')
    print_success "Zsh $zsh_ver"
    [ -d "$HOME/.oh-my-zsh" ] && echo -e "  ${GRAY}-> Oh My Zsh installed${NC}"
  else
    echo -e "${GRAY}  - Zsh (not installed)${NC}"
  fi

  # Neovim
  if command -v nvim &> /dev/null; then
    local nvim_ver
    nvim_ver=$(nvim --version | head -n1 | awk '{print $2}')
    print_success "Neovim $nvim_ver"
  else
    echo -e "${GRAY}  - Neovim (not installed)${NC}"
  fi

  # VS Code
  if command -v code &> /dev/null; then
    local code_ver
    code_ver=$(code --version 2>/dev/null | head -n1 || echo "unknown")
    print_success "VS Code $code_ver"
  else
    echo -e "  ${GRAY}- VS Code (not installed)${NC}"
  fi

  # Claude Code
  if [ -f "$HOME/.claude/settings.json" ]; then
    print_success "Claude Code (configured)"
    local skill_count
    skill_count=$(find "$HOME/.claude/skills" -name "*.md" 2>/dev/null | wc -l | tr -d ' ')
    echo -e "  ${GRAY}-> $skill_count skill(s) installed${NC}"
  else
    echo -e "${GRAY}  - Claude Code (not configured)${NC}"
  fi

  # TUI Tools
  echo ""
  echo -e "${CYAN}TUI Tools:${NC}"
  for tool in gum fzf; do
    if command -v "$tool" &> /dev/null; then
      print_success "$tool (installed)"
    else
      echo -e "${GRAY}  - $tool (not installed)${NC}"
    fi
  done

  # Sync status
  echo ""
  echo -e "${CYAN}Sync Status:${NC}"
  cd "$DOTFILES_DIR"
  if git status --porcelain 2>/dev/null | grep -q .; then
    print_info "Uncommitted changes in repository"
  else
    print_success "Repository is clean"
  fi

  local changed=0
  local missing=0
  local total=0

  for file in "${managed_dotfiles[@]}"; do
    local home_path
    local repo_path
    local label

    home_path="$(get_home_path "$file")"
    repo_path="$DOTFILES_DIR/$file"
    label="$(get_dotfile_label "$file")"
    total=$((total + 1))

    if [ ! -e "$home_path" ] || [ ! -e "$repo_path" ]; then
      missing=$((missing + 1))
      print_info "$label missing on one side"
    elif ! diff -q "$home_path" "$repo_path" >/dev/null 2>&1; then
      changed=$((changed + 1))
      print_info "$label differs"
    fi
  done

  if [ "$changed" -gt 0 ] || [ "$missing" -gt 0 ]; then
    print_info "$changed changed and $missing missing of $total managed dotfile(s)"
  else
    print_success "All $total managed dotfile(s) are synced"
  fi
}

# Health check
run_health_check() {
  print_header "System Health Check"

  local errors=0
  local warnings=0

  echo -e "${CYAN}Shell Configuration:${NC}"
  if [[ "$SHELL" == */zsh ]]; then
    print_success "Zsh is default shell"
  else
    print_info "Default shell: $SHELL (consider switching to Zsh)"
    ((warnings++))
  fi

  if [ -f "$HOME/.zshrc" ]; then print_success ".zshrc exists"; else print_error ".zshrc not found"; ((errors++)); fi

  echo ""
  echo -e "${CYAN}Development Tools:${NC}"
  for tool in git nvim code gum fzf fd jq tree rg starship zoxide pyenv uv ruff; do
    if command -v "$tool" &> /dev/null; then
      print_success "$tool installed"
    else
      print_info "$tool not installed (optional)"
      ((warnings++))
    fi
  done

  if command -v nvim &> /dev/null; then
    echo ""
    echo -e "${CYAN}Neovim Configuration:${NC}"
    if [ -f "$HOME/.config/nvim/init.lua" ]; then print_success "init.lua exists"; else print_info "init.lua not found"; ((warnings++)); fi
    if [ -d "$HOME/.local/share/nvim/lazy/lazy.nvim" ]; then print_success "lazy.nvim installed"; else print_info "lazy.nvim not installed"; ((warnings++)); fi
  fi

  echo ""
  echo -e "${CYAN}Prompt:${NC}"
  if command -v starship &> /dev/null; then
    print_success "Starship prompt installed"
    if [ -f "$HOME/.config/starship.toml" ]; then print_success "starship.toml exists"; else print_info "starship.toml not found"; ((warnings++)); fi
  else
    print_info "Starship not installed"
    ((warnings++))
  fi

  echo ""
  echo -e "${CYAN}Summary:${NC}"
  if [ "$errors" -eq 0 ] && [ "$warnings" -eq 0 ]; then
    print_success "All checks passed! System is healthy."
  elif [ "$errors" -eq 0 ]; then
    print_info "$warnings warning(s) found"
  else
    print_error "$errors error(s) and $warnings warning(s) found"
  fi
}

run_doctor() {
  print_header "Dotfiles Doctor"

  local errors=0
  local warnings=0
  local changed=0
  local missing=0
  local file repo_path home_path ahead behind

  echo -e "${CYAN}Repository:${NC}"
  if git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
    print_success "Git repository detected"
  else
    print_error "Not inside a git repository"
    echo "  Run from: $DOTFILES_DIR"
    return 1
  fi

  if git status --porcelain 2>/dev/null | grep -q .; then
    print_warning "Repository has uncommitted changes"
    echo "  Inspect: git status --short"
    warnings=$((warnings + 1))
  else
    print_success "Repository is clean"
  fi

  if git rev-parse --abbrev-ref --symbolic-full-name '@{u}' >/dev/null 2>&1; then
    read -r behind ahead < <(git rev-list --left-right --count '@{u}...HEAD')
    if [ "$ahead" -eq 0 ] && [ "$behind" -eq 0 ]; then
      print_success "Repository is synced with upstream"
    else
      print_warning "Repository differs from upstream: ahead=$ahead behind=$behind"
      [ "$behind" -gt 0 ] && echo "  Pull: git pull"
      [ "$ahead" -gt 0 ] && echo "  Push: git push"
      warnings=$((warnings + 1))
    fi
  else
    print_warning "No upstream branch configured"
    warnings=$((warnings + 1))
  fi

  echo ""
  echo -e "${CYAN}Managed dotfiles:${NC}"
  for file in "${managed_dotfiles[@]}"; do
    repo_path="$DOTFILES_DIR/$file"
    home_path="$(get_home_path "$file")"

    if [ ! -e "$repo_path" ] || [ ! -e "$home_path" ]; then
      print_error "$file missing on one side"
      echo "  Home: $home_path"
      echo "  Repo: $repo_path"
      echo "  Fix repo -> home: dotfiles apply $file"
      echo "  Fix home -> repo: dotfiles capture $file"
      missing=$((missing + 1))
      errors=$((errors + 1))
    elif ! diff -q "$home_path" "$repo_path" >/dev/null 2>&1; then
      print_warning "$file differs"
      echo "  Inspect: dotfiles diff $file"
      echo "  Apply repo -> home: dotfiles apply $file"
      echo "  Capture home -> repo: dotfiles capture $file"
      changed=$((changed + 1))
      warnings=$((warnings + 1))
    fi
  done

  if [ "$changed" -eq 0 ] && [ "$missing" -eq 0 ]; then
    print_success "All ${#managed_dotfiles[@]} managed dotfile(s) are synced"
  fi

  echo ""
  echo -e "${CYAN}Shell and prompt:${NC}"
  if [[ "$SHELL" == */zsh ]]; then
    print_success "Zsh is the default shell"
  else
    print_warning "Default shell is $SHELL, not zsh"
    echo "  Fix: dotfiles install zsh"
    warnings=$((warnings + 1))
  fi

  if [ -d "$HOME/.oh-my-zsh" ]; then print_success "Oh My Zsh installed"; else print_error "Oh My Zsh missing"; echo "  Fix: dotfiles install zsh"; errors=$((errors + 1)); fi
  if command -v starship >/dev/null 2>&1; then print_success "Starship installed"; else print_error "Starship missing"; echo "  Fix: dotfiles install zsh"; errors=$((errors + 1)); fi
  if [ -f "$HOME/.config/starship.toml" ]; then print_success "Starship config present"; else print_error "Starship config missing"; echo "  Fix: dotfiles apply zsh/starship.toml"; errors=$((errors + 1)); fi

  echo ""
  echo -e "${CYAN}iTerm2:${NC}"
  if [ "$(uname -s)" = "Darwin" ]; then
    if [ -f "$HOME/Library/Application Support/iTerm2/DynamicProfiles/hermes-nerd-font.json" ]; then print_success "Hermes Nerd Font dynamic profile installed"; else print_warning "Hermes Nerd Font dynamic profile missing"; echo "  Fix: dotfiles install iterm2"; warnings=$((warnings + 1)); fi
    if [ -f "$HOME/Library/Application Support/iTerm2/Scripts/AutoLaunch/set_hermes_nerd_font_default.py" ]; then print_success "iTerm2 AutoLaunch default-profile script installed"; else print_warning "iTerm2 AutoLaunch script missing"; echo "  Fix: dotfiles install iterm2"; warnings=$((warnings + 1)); fi
  else
    print_info "Skipped on non-macOS"
  fi

  echo ""
  echo -e "${CYAN}Neovim:${NC}"
  if command -v nvim >/dev/null 2>&1; then
    if nvim --headless '+qa' >/dev/null 2>&1; then
      print_success "Neovim opens cleanly"
    else
      print_error "Neovim startup failed"
      echo "  Inspect: nvim --headless '+qa'"
      echo "  Fix: dotfiles install nvim"
      errors=$((errors + 1))
    fi
  else
    print_warning "Neovim not installed"
    echo "  Fix: dotfiles install nvim"
    warnings=$((warnings + 1))
  fi

  echo ""
  echo -e "${CYAN}Toolchain:${NC}"
  for tool in brew git gum fzf fd rg jq starship zoxide pyenv uv ruff; do
    if command -v "$tool" >/dev/null 2>&1; then
      print_success "$tool installed"
    else
      print_warning "$tool missing"
      echo "  Fix: dotfiles install system"
      warnings=$((warnings + 1))
    fi
  done

  echo ""
  echo -e "${CYAN}Summary:${NC}"
  if [ "$errors" -eq 0 ] && [ "$warnings" -eq 0 ]; then
    print_success "Doctor found no issues."
    return 0
  fi

  [ "$warnings" -gt 0 ] && print_warning "$warnings warning(s) found"
  [ "$errors" -gt 0 ] && print_error "$errors error(s) found"
  [ "$errors" -gt 0 ] && return 1
  return 0
}

# Update installed tools
update_tools() {
  print_header "Updating Tools"

  if command -v brew &> /dev/null; then
    print_info "Updating Homebrew packages..."
    brew update && brew upgrade
  fi

  if command -v uv &> /dev/null; then
    print_info "Updating uv tools..."
    uv tool upgrade --all || true
  fi

  if command -v pyenv &> /dev/null; then
    print_info "Updating pyenv..."
    if command -v brew &> /dev/null; then
      brew upgrade pyenv || true
    else
      pyenv update || true
    fi
  fi

  if command -v nvim &> /dev/null && [ -d "$HOME/.local/share/nvim/lazy/lazy.nvim" ]; then
    print_info "Updating Neovim plugins..."
    nvim --headless "+Lazy! sync" +qa 2>/dev/null || true
  fi

  local zsh_custom="${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}"
  if [ -d "$zsh_custom/plugins" ]; then
    print_info "Updating Zsh plugins..."
    for plugin_dir in "$zsh_custom/plugins"/*/; do
      if [ -d "$plugin_dir/.git" ]; then
        local plugin_name
        plugin_name="$(basename "$plugin_dir")"
        if git -C "$plugin_dir" pull --quiet; then print_success "$plugin_name updated"; else print_warning "$plugin_name update failed"; fi
      fi
    done
  fi

  if [ -d "$HOME/.oh-my-zsh" ]; then
    print_info "Updating Oh My Zsh..."
    git -C "$HOME/.oh-my-zsh" pull --quiet || true
  fi

  print_success "All tools updated!"
}

# Main menu
show_menu() {
  if [ "$HAS_GUM" = true ]; then
    gum style \
      --border double \
      --border-foreground 212 \
      --padding "1 2" \
      --margin "1 0" \
      "Dotfiles Manager" \
      "" \
      "Manage your development environment"

    CHOICE=$(gum choose \
      "Apply Repo to Home" \
      "Capture Home to Repo" \
      "Show Diffs" \
      "Install Components" \
      "Update Tools" \
      "Show Status" \
      "Run Doctor" \
      "Run Health Check" \
      "Help" \
      "Exit")

    case "$CHOICE" in
      "Apply Repo to Home") apply_repo_to_home ;;
      "Capture Home to Repo") capture_home_to_repo ;;
      "Show Diffs") show_dotfile_diffs ;;
      "Install Components") "$DOTFILES_DIR/bootstrap.sh" ;;
      "Update Tools") update_tools ;;
      "Show Status") show_status ;;
      "Run Doctor") run_doctor ;;
      "Run Health Check") run_health_check ;;
      "Help") show_help ;;
      "Exit") echo "Goodbye!"; exit 0 ;;
    esac
  else
    print_header "Dotfiles Manager"
    echo "1) Apply Repo to Home"
    echo "2) Capture Home to Repo"
    echo "3) Show Diffs"
    echo "4) Install Components"
    echo "5) Update Tools"
    echo "6) Show Status"
    echo "7) Run Doctor"
    echo "8) Run Health Check"
    echo "9) Help"
    echo "10) Exit"
    echo ""
    read -rp "Choose an option (1-10): " choice

    case "$choice" in
      1) apply_repo_to_home ;;
      2) capture_home_to_repo ;;
      3) show_dotfile_diffs ;;
      4) "$DOTFILES_DIR/bootstrap.sh" ;;
      5) update_tools ;;
      6) show_status ;;
      7) run_doctor ;;
      8) run_health_check ;;
      9) show_help ;;
      10) echo "Goodbye!"; exit 0 ;;
      *) echo "Invalid option"; exit 1 ;;
    esac
  fi
}

# Main command router
cd "$DOTFILES_DIR"

case "${1:-menu}" in
  sync)
    shift
    "$DOTFILES_DIR/update_dotfiles.sh" "$@"
    ;;
  apply)
    shift
    apply_repo_to_home "$@"
    ;;
  capture)
    shift
    capture_home_to_repo "$@"
    ;;
  diff)
    shift
    show_dotfile_diffs "$@"
    ;;
  list)
    list_managed_dotfiles
    ;;
  install)
    shift
    "$DOTFILES_DIR/bootstrap.sh" "$@"
    ;;
  status)
    show_status
    ;;
  doctor)
    run_doctor
    ;;
  health)
    run_health_check
    ;;
  update)
    update_tools
    ;;
  help|--help|-h)
    show_help
    ;;
  menu|"")
    show_menu
    ;;
  *)
    echo -e "${RED}Unknown command: $1${NC}"
    echo "Run 'dotfiles help' for usage information"
    exit 1
    ;;
esac
